# Changelog

All notable changes to `@mlagie/sql-connector` are documented here.

## [Unreleased]

### Documentation

- Reorganized the main README with a professional table of contents and clearer API sections.
- Documented the complete schema field vocabulary, including `precision`, `scale`, `oldname`, enums, defaults and foreign keys.
- Documented structured conditions, including comparison operators, `IN`, `NOT IN`, `BETWEEN`, `NOT BETWEEN`, `NULL`, `AND`, `OR` and `NOT`.
- Added dedicated references for queries, schemas, migrations, API exports and SQL dialects.
- Added a complete migration reference covering planning, operations, approvals, destructive changes, checksums, encryption, backups, retention and status.
- Added a French README with matching structure.
