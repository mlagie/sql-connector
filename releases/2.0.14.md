# Release v2.0.14

## Fix

- Resolved a TypeScript typing issue that prevented access to the find function in certain development environments.
- Updated type declarations to ensure the find method is properly exposed and recognized by TypeScript tooling.

## Impact

This fix restores the expected developer experience when using @mlagie/sql-connector in TypeScript projects and allows the find function to be used normally again.
